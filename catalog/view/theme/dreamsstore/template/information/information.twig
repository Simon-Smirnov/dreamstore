{{ header }}
<div id="information-information" class="information_id_{{ information_id }}">
    <div class="container">
        <ul class="breadcrumb">
            {% for breadcrumb in breadcrumbs %}
                <li><a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a></li>
            {% endfor %}
        </ul>
    </div>
    <div id="content">
        <div class="container"><h1 class="page-title">{{ heading_title }}</h1></div>
        {{ description }}
        {#            {{ column_right }} #}
        {#            {{ column_left }} #}
        {#            {{ content_bottom }} #}
        {#            {{ content_top }} #}
    </div>
</div>
{{ footer }}